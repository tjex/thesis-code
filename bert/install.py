# install script for first time user


conda_packages = ["conda-forge", "sentence-transformers"]
pip_packages = ["nltk"]



# conda install -c ${conda_packages}
# pip install --user -U ${pip_packages}
